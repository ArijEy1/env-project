import { AccountPanel } from '../../components/account-panel';

export default function AccountPage() {
  return (
    <main className="page-shell auth-background-page account-background-page">
      <AccountPanel />
    </main>
  );
}
